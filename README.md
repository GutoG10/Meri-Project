Para rodar o projeto faça o comando no terminal na pasta do projeto:

python -m venv venv
source venv/bin/activate caso for Linux/macOS
venv\Scripts\activate caso for Windows

pip install -r requirements.txt

python .\src\detect_stop.py

pressione ESC para fechar o programa
